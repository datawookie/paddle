<ul class="nav flex-column">
    <li class="nav-item d-grid gap-2">
        <a href="{{ url_for('kanoe.paddler') }}" class="btn btn-primary">Add a Paddler</a>
    </li>
    <li class="nav-item d-grid gap-2">
        <a href="{{ url_for('kanoe.paddlers_validate') }}" class="btn btn-primary">Validate Paddlers</a>
    </li>
</ul>

{% if paddler %}
<p>Paddler ID: {{ paddler.id }}</p>
{% endif %}
