<nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
    <a class="navbar-brand" href="{{ url_for('paddle.races') }}">paddle</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExampleDefault"
        aria-controls="navbarsExampleDefault" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarsExampleDefault">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ url_for('paddle.races') }}">Home</a>
            </li>
            {% if session["user"] or config['LOGIN_DISABLED'] %}
            <li class="nav-item active">
                <a class="nav-link" href="{{ url_for('paddle.paddlers') }}">Paddlers</a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ url_for('paddle.teams') }}">Teams</a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ url_for('paddle.clubs') }}">Clubs</a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ url_for('paddle.announcement') }}">Announcement</a>
            </li>
            {% endif %}
            {% if not config['LOGIN_DISABLED'] %}
            {% if session["user"] %}
            <li class="nav-item active">
                <a class="nav-link" href="{{ url_for('paddle.logout') }}">Logout</a>
            </li>
            {% else %}
            <li class="nav-item active">
                <a class="nav-link" href="{{ url_for('paddle.login') }}">Login</a>
            </li>
            {% endif %}
            {% endif %}
            <!--
            <li class="nav-item active">
                <a class="nav-link" href="{{ url_for('paddle.time_trial') }}">Time Trial Results</a>
            </li>
            -->
        </ul>
    </div>
</nav>
