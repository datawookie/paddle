{% extends "base-minimal.j2" %}

{% block styles %}
<link rel="stylesheet" type="text/css" href="/css/paginated-a4-portrait.css">
{% endblock %}

{% block footer %}
<div style="width: 100%;">
    <div class="title">
        {{ series.name }}
    </div>
    <div class="timestamp">
        {{ timestamp.strftime('%Y-%m-%d %H:%M:%S') }}
    </div>
</div>
{% endblock %}

{% block content %}
{% for category, results in categories.items() %}
<div class="break">
    <h1 style="text-align: center;">{{ series.name }}</h1>

    <h2>{{ category }}</h2>

    <table>
        {% for result in results %}
        <tr>
            <td class="position">{{ loop.index }}</td>
            <td class="paddler">{{ result.name }}</td>
            <td class="time">{{ result.time }}</td>
        </tr>
        {% endfor %}
    </table>
</div>
{% endfor %}
{% endblock %}
