<ul class="nav flex-column">
    <li class="nav-item mb-2">
        <a href="{{ url_for('kanoe.number_update', entry_id=entry.id) }}" class="btn btn-primary">Update Race Number</a>
    </li>
</ul>
<ul class="nav flex-column">
    <li class="nav-item mb-2">
        <a href="{{ url_for('kanoe.entry_note_add', entry_id=entry.id) }}" class="btn btn-primary">Add Note</a>
    </li>
    <li class="nav-item mb-2">
        <a href="{{ url_for('kanoe.entry_note_delete', entry_id=entry.id) }}" class="btn btn-primary">Delete Note</a>
    </li>
</ul>

<p>Entry ID: {{ entry.id }}</p>